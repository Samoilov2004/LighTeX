# Simple Presentation

Edit the metadata at the top of `main.tex`, duplicate frames as needed, and
keep each slide focused on one message. The template uses only standard
TeX Live packages and does not require an external Beamer theme.

Build with:

```bash
make pdf
```
